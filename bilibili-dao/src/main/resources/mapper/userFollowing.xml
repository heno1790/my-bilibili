<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--DAO层是直接与数据库进行交互的层,xml文件自然是写道这一层模块中了-->
<mapper namespace="com.tt.dao.UserFollowingDao">
    <insert id="addUserFollowing" parameterType="com.tt.domain.UserFollowing">
        insert into
            t_user_following(
                userId,
                followingId,
                groupId,
                createTime
        )values(
                #{userId},
                #{followingId},
                #{groupId},
                #{createTime}
               )
    </insert>

    <delete id="deleteUserFollowing">
        delete from
            t_user_following
        where
            userId = #{userId} and followingId = #{followingId}
    </delete>

    <select id="getUserFollowingByUserId" resultType="com.tt.domain.UserFollowing">
        select *
        from t_user_following
        where userId = #{userId}
    </select>

    <select id="getUserFans" resultType="com.tt.domain.UserFollowing" parameterType="java.lang.Long">
        select *
        from t_user_following
        where followingId = #{followingId}
    </select>

</mapper>
